 server {
    # Server mendengarkan di port 8080
    listen 8080;
    listen [::]:8080;

    root /var/www/html/mysite;
    index index.html;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}
